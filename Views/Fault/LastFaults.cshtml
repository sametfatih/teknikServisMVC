@using teknikServisMVC.Models.Entities.Concrete;
@model ICollection<Ariza>;

<div class="col-lg-6 col-md-6 col-sm-6">
    <div class="card">
        <div class="header">
            <h2><strong>*En Son</strong> Arıza Kayıtları</h2>
        </div>
        <div class="body">
            <p>En son kaydedilen arıza kayıtları.</p>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Müşteri</th>
                            <th>Cihaz Modeli</th>
                            <th>Açıklama</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            @foreach (var fault in Model)
                            {
                                <tr>
                                    <th scope="row">@fault.Id</th>
                                    <td>@fault.Musteri.Ad @fault.Musteri.Soyad </td>
                                    <td>@fault.Cihaz.Model</td>
                                    <td>@fault.Aciklama</td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>